//runs once
function setup(){
  //specify a width and height for our drawing area
  createCanvas(windowWidth, windowHeight)
}

//runs on a loop
function draw(){
  ellipse(windowWidth/2, windowHeight/2, 50)
}
